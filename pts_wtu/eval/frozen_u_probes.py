"""Frozen-U probes: action / state / phase / progress (plan §36)."""
from __future__ import annotations


def run_frozen_u_probes(latents_dir: str, records_dir: str, out_dir: str) -> dict:
    raise NotImplementedError
